name: Prepare-Release

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v6
      with:
        go-version: stable
    - name: prepare-release
      shell: bash
      run: |
        set -eu
        make generate
